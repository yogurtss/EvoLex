# EvoLex data contracts package.
#
# This package defines the typed objects that replace the original
# free-form semantic_atom dicts. Every extraction node produces
# these typed objects, and downstream nodes consume them.
